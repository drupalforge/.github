{
    "name": "Docker build and push template",
    "description": "Commits a new Docker image when a commit is pushed to the default branch. The image will be published to the Drupal Forge Docker Hub account by default.",
    "iconName": "docker-mark-blue",
    "categories": [
        "deployment"
    ],
    "filePatterns": [
        "^\\.devpanel$"
    ]
}
